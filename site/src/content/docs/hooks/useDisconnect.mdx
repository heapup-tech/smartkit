---
title: useDisconnect
description: Hook for disconnecting connections.
---

```tsx
import { useDisconnect } from '@heapup/smartkit-hooks'

const { disconnect } = useDisconnect()
```

When the disconnect method is called, it will automatically disconnect from the current wallet.
