---
title: useConnect
description: Manually connect wallet
---

```tsx
import { useConnect } from '@heapup/smartkit-hooks'
const { connect, connectAsync } = useConnect()
```

`connect` and `connectAsync` functions require a wallet parameter
